include LICENSE
include README.md
include SECURITY.md

# Defensive: never let an auto-downloaded tor binary get pulled into an sdist
# if a developer has run the package locally.
prune src/basic_tor/bin
global-exclude *.pyc
global-exclude __pycache__
